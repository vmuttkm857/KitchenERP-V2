# KitchenERP 資料模型

> V2 PostgreSQL 實際 migration head：`20260901_0012`。下方原 V1 A/B 盤點保留為歷史設計來源；V2 實際 schema 以 `backend/migrations/versions/` 與 SQLAlchemy models 為準，不支援 SQLite。

## V2 標準食譜卡／廚房標準製程（0012）

- `dish_production_profiles`：每道菜的最大單批量、整體注意事項與私有成品照片 metadata。
- `production_batch_versions`：同一道菜可保存多個人工確認份數版本，並區分正式／草稿。
- `production_batch_ingredients`：由既有 `dish_ingredients` 建議值建立，保存該份數人工校正量與加入用途；不取代原 Recipe BOM。
- `production_process_steps`：版本所屬的有序製程、設備、時間、溫度與批量／盤量設定。

## V2 Nutrition Domain（0010）

- `nutrition_foods`：`tfda`／`manual` 食品主檔；TFDA 以 `(source, external_code)` 唯一，manual 可沒有 external code。
- `nutrition_nutrients`：動態營養素定義；`corrected_energy` 固定代表「修正熱量」、`kcal`、`per_100_g`。
- `nutrition_food_values`：`(food_id, nutrient_id)` 唯一，值為 `NUMERIC(24,10)`；blank 不建立 row，與數值 0 明確分離。
- `nutrition_import_batches`：只保存版本、原始安全檔名、header row、hash、counts、actor 與狀態，不保存 XLSX binary。
- `ingredients.nutrition_food_id`：nullable、`ON DELETE RESTRICT`；營養來源狀態由對應食品的 source 推導，不重複保存。
- 官方匯入只更新 Nutrition tables；不修改 Ingredient、Recipe、Kitchen、Requirement 或 Purchase 資料與計算。

## V2 Nutrition Unit Conversion（0011）

- `ingredient_nutrition_unit_conversions`：Ingredient 專屬、只供 Nutrition 計算使用；保存 normalized `unit` 與 `grams_per_unit NUMERIC(24,10)`。
- `(ingredient_id, unit)` 唯一且 `grams_per_unit > 0`；created/updated actor 使用 `ON DELETE RESTRICT`。
- Ingredient 合法永久刪除時換算列 `ON DELETE CASCADE`；此表不被 Purchase、Requirement、Kitchen Operations、成本或訂購邏輯讀取。
- 計算優先使用原生 `g/kg/斤`，其他單位（含 `ml/L`）只查此表的明確換算，不建立全域密度假設。

## V2 Users 與 Audit（0009）

### `users`

- UUID primary key；`username` unique；只保存 Argon2 `password_hash`，不保存或回傳明碼。
- `role` 由 `ck_users_role` 限制為 `admin` / `user`；`is_active` 控制登入與目前 access-token principal 是否仍有效。
- `created_by` / `updated_by` self-FK 使用 `ON DELETE RESTRICT`，保存快速 actor 資訊。
- 使用者不提供 hard-delete；停用、重設密碼、自行改密碼會撤銷 refresh sessions。

### `audit_logs`

| 欄位 | PostgreSQL 型別 | 規則 |
|---|---|---|
| id | UUID | primary key |
| actor_user_id | UUID nullable | FK → users.id, `ON DELETE RESTRICT` |
| actor_username / actor_display_name | VARCHAR | 操作當時 snapshot，不受日後 User 修改影響 |
| action / entity_type | VARCHAR | 穩定的操作與資料類型識別 |
| entity_id / entity_label | UUID nullable / VARCHAR nullable | 被操作資料識別與人類可讀名稱 |
| before_data / after_data | JSONB nullable | 集中 sanitization 後的修改前／後內容 |
| metadata | JSONB NOT NULL | 非敏感操作脈絡，預設 `{}` |
| request_id / ip_address | VARCHAR nullable | request correlation 與直接 peer IP |
| created_at | TIMESTAMPTZ | server default current timestamp |

索引：`created_at`、`(actor_user_id, created_at)`、`(action, created_at)`、`(entity_type, entity_id, created_at)`。Application API 僅提供 admin 查詢，不提供 PATCH/DELETE；Audit 與業務操作在同一 transaction 寫入。

---

## V1 歷史資料模型盤點

> 本文件區分「B 所代表的 SQLite 原始/升級後模型」與「A 的 PostgreSQL target schema」。SQLite 基礎建表在 `database/init_db.py`，實際 V1 功能還依賴 `migrate_*.py`；因此只看 init_db 不足以描述真實模型。

## 1. Schema 比較結論

### B：SQLite 原始語意（含正式功能 migrations）

- IDs：`INTEGER PRIMARY KEY AUTOINCREMENT`。
- 日期/時間：`TEXT`（ISO string/CURRENT_TIMESTAMP）。
- 數量/金額：`REAL`。
- boolean：`INTEGER`，慣例 `1/0`。
- JSON：`TEXT`，以 JSON string 保存。
- FK：部分基礎/後續 table 有 FK；SQLite 需連線啟用 `PRAGMA foreign_keys = ON` 才真正執行。

### A：PostgreSQL target schema

- IDs：`INTEGER GENERATED BY DEFAULT AS IDENTITY`。
- 日期/時間：`DATE` / `TIMESTAMPTZ`。
- 數量/金額：`NUMERIC(18,6)` 或 `NUMERIC(18,4)`，應對應 Python `Decimal`。
- boolean：`BOOLEAN`。
- JSON：`JSONB`，psycopg 讀回 list/dict。
- FK/UNIQUE/CHECK/ON DELETE：在 schema 中明確約束；見 `database/postgresql/001_initial_schema.sql`。

**V2 建議**：使用 PostgreSQL target 的語意，並把 SQLite-specific 型別/PRAGMA/migration 補丁全部留在歷史文件，不帶入 V2。

## 2. 主檔資料表

### `categories`

| 欄位 | B SQLite | A PostgreSQL | 約束/關係 |
|---|---|---|---|
| category_id | INTEGER PK AUTOINCREMENT | INTEGER identity PK | 食材分類 identity |
| category_name | TEXT NOT NULL | TEXT NOT NULL | UNIQUE |
| is_active | INTEGER default 1 | BOOLEAN default TRUE | 停用不改既有關聯 |
| created_at / updated_at | TEXT CURRENT_TIMESTAMP | TIMESTAMPTZ CURRENT_TIMESTAMP | 建立/更新時間 |

### `suppliers`

| 欄位 | B | A | 約束/關係 |
|---|---|---|---|
| supplier_id | INTEGER PK | INTEGER identity PK | 供應商 identity |
| supplier_code | TEXT | TEXT | NOT NULL, UNIQUE |
| supplier_name | TEXT | TEXT | NOT NULL |
| contact_person, phone, notes | TEXT nullable | TEXT nullable | 聯絡/備註 |
| is_active | INTEGER default 1 | BOOLEAN default TRUE | 狀態 |
| created_at / updated_at | TEXT | TIMESTAMPTZ | 時間 |

### `dish_categories`

由 `migrate_dish_categories.py` 加入 SQLite；A PostgreSQL schema 直接建立。

| 欄位 | B | A | 約束 |
|---|---|---|---|
| dish_category_id | INTEGER PK | INTEGER identity PK | identity |
| dish_category_name | TEXT NOT NULL | TEXT NOT NULL | UNIQUE |
| is_active | INTEGER default 1 | BOOLEAN default TRUE | 狀態 |
| created_at, updated_at | TEXT | TIMESTAMPTZ | 时间 |

### `menu_categories`

| 欄位 | B | A | 約束 |
|---|---|---|---|
| menu_category_id | INTEGER PK | INTEGER identity PK | identity |
| menu_category_name | TEXT NOT NULL | TEXT NOT NULL | UNIQUE |
| sort_order | INTEGER default 0 | INTEGER default 0 | UI order |
| is_active | INTEGER default 1 | BOOLEAN default TRUE | 狀態 |
| created_at, updated_at | TEXT | TIMESTAMPTZ | 時間 |

### `ingredients`

| 欄位 | B SQLite（init+migration） | A PostgreSQL | 關係/規則 |
|---|---|---|---|
| ingredient_id | INTEGER PK | INTEGER identity PK | identity |
| ingredient_code | TEXT NOT NULL | TEXT NOT NULL | UNIQUE |
| ingredient_name | TEXT NOT NULL | TEXT NOT NULL | 名稱 |
| category_id | INTEGER NOT NULL | INTEGER NOT NULL | → categories, PostgreSQL `ON DELETE RESTRICT` |
| unit | TEXT NOT NULL | TEXT NOT NULL | 食材基本單位 |
| current_price | REAL default 0 | NUMERIC(18,4) default 0 | 非負 CHECK（A） |
| primary_supplier_id | INTEGER nullable | INTEGER nullable | → suppliers, A `ON DELETE RESTRICT` |
| purchase_unit | TEXT nullable（migration） | TEXT nullable | 採購單位 |
| package_size | REAL default 1（migration） | NUMERIC(18,6) default 1 | 非負 CHECK（A） |
| minimum_order_quantity | REAL default 0（migration） | NUMERIC(18,6) default 0 | 非負 CHECK（A） |
| notes | TEXT | TEXT | 備註 |
| is_active | INTEGER default 1 | BOOLEAN default TRUE | 狀態 |
| created_at, updated_at | TEXT | TIMESTAMPTZ | 時間 |

### `ingredient_price_history`

| 欄位 | B | A | 關係/規則 |
|---|---|---|---|
| price_history_id | INTEGER PK | INTEGER identity PK | identity |
| ingredient_id | INTEGER NOT NULL | INTEGER NOT NULL | → ingredients; A restrict |
| supplier_id | INTEGER nullable | INTEGER nullable | → suppliers; A restrict |
| price | REAL NOT NULL | NUMERIC(18,4) NOT NULL | A 非負 CHECK |
| unit | TEXT NOT NULL | TEXT NOT NULL | 價格單位 |
| effective_date | TEXT NOT NULL | DATE NOT NULL | 生效日 |
| notes | TEXT | TEXT | 備註 |
| created_at | TEXT | TIMESTAMPTZ | 時間 |

## 3. 菜色與配方

### `dishes`

| 欄位 | B | A | 關係/規則 |
|---|---|---|---|
| dish_id | INTEGER PK | INTEGER identity PK | identity |
| dish_code | TEXT NOT NULL | TEXT NOT NULL | UNIQUE |
| dish_name | TEXT NOT NULL | TEXT NOT NULL | UNIQUE |
| notes | TEXT | TEXT | 備註 |
| is_active | INTEGER default 1 | BOOLEAN default TRUE | 狀態 |
| dish_category_id | INTEGER nullable（migration） | INTEGER nullable | → dish_categories; A `ON DELETE SET NULL` |
| created_at, updated_at | TEXT | TIMESTAMPTZ | 時間 |

### `dish_ingredients`

| 欄位 | B | A | 關係/規則 |
|---|---|---|---|
| dish_ingredient_id | INTEGER PK | INTEGER identity PK | identity |
| dish_id | INTEGER NOT NULL | INTEGER NOT NULL | → dishes; A restrict |
| ingredient_id | INTEGER NOT NULL | INTEGER NOT NULL | → ingredients; A restrict |
| quantity | REAL NOT NULL | NUMERIC(18,6) NOT NULL | A `>= 0` CHECK |
| unit | TEXT NOT NULL | TEXT NOT NULL | **配方使用單位** |
| loss_rate | REAL default 0（migration） | NUMERIC(9,6) default 0 | A `>= 0` CHECK；百分比語意 |
| notes | TEXT | TEXT | 配方備註 |
| created_at, updated_at | TEXT | TIMESTAMPTZ | 時間 |
| unique | `(dish_id, ingredient_id)` | 同 | 一道菜一食材僅一列 |

## 4. 菜單資料

### `menus`

| 欄位 | B | A | 關係/規則 |
|---|---|---|---|
| menu_id | INTEGER PK | INTEGER identity PK | identity |
| menu_name | TEXT NOT NULL | TEXT NOT NULL | 菜單名稱 |
| menu_start_date / menu_end_date | TEXT NOT NULL | DATE NOT NULL | A end >= start CHECK |
| notes | TEXT | TEXT | 備註 |
| menu_category_id | INTEGER nullable（migration） | INTEGER nullable | → menu_categories; A SET NULL |
| is_active | INTEGER default 1 | BOOLEAN default TRUE | 狀態 |
| created_at, updated_at | TEXT | TIMESTAMPTZ | 時間 |

### `menu_meal_types`

| 欄位 | B | A | 關係/規則 |
|---|---|---|---|
| menu_meal_type_id | INTEGER PK | INTEGER identity PK | identity |
| menu_id | INTEGER NOT NULL | INTEGER NOT NULL | → menus; A restrict |
| meal_type | TEXT NOT NULL | TEXT NOT NULL | 同 menu 唯一 |
| sort_order | INTEGER default 1 | INTEGER default 1 | 餐別次序 |
| is_active | INTEGER default 1 | BOOLEAN default TRUE | 停用不刪記錄 |
| created_at, updated_at | TEXT | TIMESTAMPTZ | 時間 |
| unique | `(menu_id, meal_type)` | 同 | 防重複 |

### `menu_days`

| 欄位 | B | A | 關係/規則 |
|---|---|---|---|
| menu_day_id | INTEGER PK | INTEGER identity PK | identity |
| menu_id | INTEGER NOT NULL | INTEGER NOT NULL | → menus; A restrict |
| menu_date | TEXT NOT NULL | DATE NOT NULL | 在菜單區間內由服務層維持 |
| meal_type | TEXT NOT NULL | TEXT NOT NULL | 與 menu meal type 名稱對應 |
| notes | TEXT | TEXT | 餐格備註 |
| unique | `(menu_id, menu_date, meal_type)` | 同 | 日期×餐別唯一 |

### `menu_dishes`

| 欄位 | B | A | 關係/規則 |
|---|---|---|---|
| menu_dish_id | INTEGER PK | INTEGER identity PK | identity |
| menu_day_id | INTEGER NOT NULL | INTEGER NOT NULL | → menu_days; A restrict |
| dish_id | INTEGER NOT NULL | INTEGER NOT NULL | → dishes; A restrict |
| diner_count | INTEGER default 1 | INTEGER default 1 | A `>=0` CHECK |
| notes | TEXT | TEXT | 餐格菜色備註 |
| sort_order | INTEGER nullable（migration） | INTEGER nullable | 同餐格排序 |
| unique | `(menu_day_id, dish_id)` | 同 | 同餐別日不重複菜色 |
| timestamps | TEXT | TIMESTAMPTZ | 時間 |

## 5. 需求快照與採購

### `requirement_snapshots`

| 欄位 | B | A | 語意 |
|---|---|---|---|
| snapshot_id | INTEGER PK | INTEGER identity PK | 快照 identity |
| snapshot_name | TEXT nullable | TEXT nullable | 人工名稱 |
| target_menus | TEXT JSON NOT NULL | JSONB NOT NULL | 選取菜單 hard copy |
| target_dates | TEXT JSON NOT NULL | JSONB NOT NULL | 需求日期 hard copy |
| total_estimated_cost | REAL default 0 | NUMERIC(18,4) default 0 | A 非負 CHECK |
| created_at | TEXT | TIMESTAMPTZ | 建立時間 |

### `requirement_snapshot_items`

快照品項包含：`snapshot_item_id` PK、`snapshot_id` FK cascade、可空的 `ingredient_id`/ingredient_code、不可空 ingredient_name、supplier_name、diner_count、quantity_per_person、loss_rate、不可空 required_quantity/unit/purchase_quantity、adjusted_quantity、purchase_unit、package_size、minimum_order_quantity、unit_price、estimated_cost、needs_review、created_at。

- B 用 REAL/INTEGER/TEXT；A 用 NUMERIC/BOOLEAN/TIMESTAMPTZ。
- A 對所有數量、成本與 diner_count 設非負 CHECK。
- 這張表刻意保存文字與數值 hard copy；ingredient_id 是追蹤資訊，不應讓日後食材變更改寫快照。

### `requirement_snapshot_item_schedules`

| 欄位 | B（含 source tracking migration） | A | 語意 |
|---|---|---|---|
| schedule_id | INTEGER PK | INTEGER identity PK | 日程 identity |
| snapshot_item_id | INTEGER NOT NULL FK cascade | 同 | 所屬快照品項 |
| requirement_date / delivery_date | TEXT NOT NULL | DATE NOT NULL | 需求與預設送貨日期 |
| allocated_quantity | REAL NOT NULL | NUMERIC(18,6) NOT NULL | A 非負 CHECK |
| menu_id / menu_name | migration 新增 nullable | nullable | 來源菜單追蹤，不是 FK |
| created_at / updated_at | TEXT | TIMESTAMPTZ | 時間 |

### `purchase_orders`

| 欄位 | B | A | 語意 |
|---|---|---|---|
| purchase_order_id | INTEGER PK | INTEGER identity PK | 正式叫貨單 identity |
| purchase_order_name | TEXT | TEXT | 名稱 |
| source_snapshot_id / source_snapshot_name | nullable INTEGER/TEXT | 同 | 來源追蹤，**刻意非 FK** |
| notes | TEXT | TEXT | 備註 |
| total_estimated_cost | REAL default 0 | NUMERIC(18,4) default 0 | A 非負 CHECK |
| status | migration `open` default | TEXT default `open` | open/closed |
| closed_at | migration TEXT nullable | TIMESTAMPTZ nullable | 結案時間 |
| created_at/updated_at | TEXT | TIMESTAMPTZ | 時間 |

### `purchase_order_items`

正式叫貨品項：PK `purchase_order_item_id`；`purchase_order_id` FK cascade；source snapshot item id；ingredient/supplier IDs 與代碼名稱 hard-copy；required/system/snapshot adjusted/ordered quantity；系統/採購單位、package/min-order、unit price、estimated cost、needs_review、timestamps。

- A 將數值轉 NUMERIC、review 轉 BOOLEAN，並對所有數字加非負 CHECK。
- ingredient/supplier source IDs 沒有 FK，是為保留歷史快照與日後主檔變動的可追溯性。

### `purchase_order_allocations`

| 欄位 | B | A | 語意 |
|---|---|---|---|
| purchase_order_allocation_id | INTEGER PK | INTEGER identity PK | allocation identity |
| purchase_order_item_id | INTEGER NOT NULL FK cascade | 同 | 所屬正式品項 |
| source_schedule_id | nullable INTEGER | nullable INTEGER | 來源日程追蹤，不是 FK |
| requirement_date / delivery_date | TEXT NOT NULL | DATE NOT NULL | 不可混淆 |
| allocated_quantity | REAL NOT NULL | NUMERIC(18,6) NOT NULL | A 非負 CHECK |
| unit | TEXT NOT NULL | TEXT NOT NULL | allocation 單位 |
| allocation_type | TEXT default demand | TEXT default demand | demand/delivery 語意 |
| notes | TEXT | TEXT | 備註 |
| menu_id / menu_name | migration nullable | nullable | 來源菜單追蹤 |
| timestamps | TEXT | TIMESTAMPTZ | 時間 |

## 6. Index 與效能語意

A PostgreSQL schema 額外明確建立：

- `idx_ingredients_active_code`：`(is_active, lower(ingredient_code))`
- `idx_ingredients_active_supplier_code`：`(is_active, primary_supplier_id, lower(ingredient_code))`
- `idx_ingredient_price_history_ingredient_date`
- `idx_dish_ingredients_dish_recipe_order`
- `idx_menu_dishes_day_sort`
- snapshot items/schedules、purchase orders/items/allocations 的查詢型 indexes。

SQLite migrations 也建立相近的 recipe、ingredient search、menu dish sort indexes。V2 應從預期查詢與 PostgreSQL `EXPLAIN ANALYZE` 驗證後保留/調整，不把舊 SQLite index 名稱視為設計本身。

## 7. 文字 ER 關係

```text
categories 1 ──< ingredients >── 0..1 suppliers
ingredients 1 ──< ingredient_price_history >── 0..1 suppliers

dish_categories 0..1 ──< dishes 1 ──< dish_ingredients >── 1 ingredients

menu_categories 0..1 ──< menus 1 ──< menu_meal_types
menus 1 ──< menu_days 1 ──< menu_dishes >── 1 dishes

requirement_snapshots 1 ──< requirement_snapshot_items 1 ──< requirement_snapshot_item_schedules

purchase_orders 1 ──< purchase_order_items 1 ──< purchase_order_allocations
purchase_orders.source_snapshot_id ── tracks ──> requirement_snapshots.snapshot_id (non-FK hard-copy origin)
purchase_order_items.source_snapshot_item_id ── tracks ──> requirement_snapshot_items.snapshot_item_id (non-FK)
purchase_order_allocations.source_schedule_id ── tracks ──> requirement_snapshot_item_schedules.schedule_id (non-FK)
```

## 8. 差異分類與 V2 決策

| 差異 | 類型 | V2 決策 |
|---|---|---|
| TEXT date/time → DATE/TIMESTAMPTZ | DB engine/type 改善 | 採 PostgreSQL typed 欄位 |
| REAL → NUMERIC | 精度改善 | 採 Decimal/NUMERIC |
| INTEGER 1/0 → BOOLEAN | DB engine | 採 BOOLEAN |
| TEXT JSON → JSONB | DB engine/function 改善 | 採 JSONB，API 明確 normalise |
| `dish_category_id`, `menu_category_id`, purchase rules, snapshot/purchase tables | 真正 V1 資料模型能力（透過 SQLite migrations 演進） | 保留 |
| PostgreSQL CHECK、RESTRICT、SET NULL、CASCADE | 資料安全改善 | 採用並以 product policy 確認 delete 行為 |

## 9. 待確認

- SQLite 現場資料庫是否已套完每一支 migration、以及每個歷史資料的實際 null/舊單位狀況，需以 **read-only schema/data audit** 另行確認。
- `dish_ingredients.quantity=0` 目前 PostgreSQL schema 允許、部分 UI 要求正值；V2 必須決定新增草稿與正式發布各自的規則。
